State Diagram.txt

Project: Hybrid Intelligence Mentor (HIM)
Repository: https://github.com/tiumbj/Hybrid_Intelligence_Mentor
Symbol (MT5): GOLD
Current Phase: Phase 9 — Live Forward Execution Stabilization
Latest Integrated Versions:
- mentor_executor.py: v2.6.1 (Hybrid SL Manager Mode C + AI Spec v2 Mode D)
- ai_mentor.py: v2.1.1 (New-trade Spec v2 Mode D + Position SL tighten-only)

IMPORTANT NOTE (Format):
- This is a “textual state diagram” (NOT Mermaid code) แต่ใช้คำว่า state / note / arrow ตามสเปคเพื่อให้เห็นภาพรวม
- Status Legend:
  ✅ Done / 🟡 In Progress / ⏳ Pending / 🔴 Issues

================================================================================
I) PROJECT ENTRY + HIGH-LEVEL FLOW (TEXT STATE FLOW)
================================================================================

[*] --> Project_Init

state "Project_Init" : ✅ Done
note right of Project_Init
- Repo initialized, runbook/docs present
- MT5 Python integration available
- Base config system exists
end note

Project_Init --> Config_System
Config_System --> Strategy_Engine
Strategy_Engine --> (Gate: Frozen Rules + Safety Guards)
(Gate: Frozen Rules + Safety Guards) --> AI_Layer_ModeD
AI_Layer_ModeD --> Execution_Controller
Execution_Controller --> Position_Manager_ModeC
Position_Manager_ModeC --> State_And_Logging
State_And_Logging --> Monitoring_UI
Monitoring_UI --> [*] (loop)

note right of (Gate: Frozen Rules + Safety Guards)
- Frozen: mode=sideway_scalp, min_rr=1.5, ATR SL window 1.2–1.8, confirmation required
- Spread / stops_level / freeze_level / cooldown / stacking guard
end note

note right of Position_Manager_ModeC
- Python manages: BE@0.6R, Trail>=1R, Emergency SL on flip
- AI assists: tighten-only SL (NEVER widen), only when in profit + high-risk trigger
end note

================================================================================
II) FULL FILE STRUCTURE (FROM git ls-files) + MAIN FUNCTION + STATUS + WHY/NUMBERS
================================================================================

----------------------------------------
A) Root / Docs / Procedures
----------------------------------------

state "README_TH.md" : ✅ Done
note right of README_TH.md
- Project overview in Thai
end note

state "instructions.txt" : ✅ Done
state "instructions.html" : ✅ Done
state "Run procedure.html" : ✅ Done
state "Code Run Test.txt" : 🟡 In Progress
note right of Code Run Test.txt
- Needs update to reflect latest versions v2.6.1 + v2.1.1 and new debug workflow
end note

state ".gitignore" : ✅ Done
state "bu.txt" : 🟡 In Progress
note right of bu.txt
- Utility/notes; not part of runtime critical path
end note

state "remove_emoji.py" : ✅ Done
note right of remove_emoji.py
- Text cleanup utility (non-runtime)
end note

----------------------------------------
B) Config System (Core)
----------------------------------------

state "Config_System" : ✅ Done
Config_System --> Strategy_Engine
Config_System --> Execution_Controller
Config_System --> AI_Layer_ModeD
Config_System --> Monitoring_UI

state "config.json" : ✅ Done
note right of config.json
Key frozen numbers (current):
- symbol: "GOLD"
- mode: "sideway_scalp"
- min_rr: 1.5
- SL ATR window: 1.2–1.8 (AI constraints + Python validation)
- confidence_threshold: 70 (trade gate; can temporarily lower for testing)
Execution/Multi-order typical keys:
- allow_multiple_positions, max_positions_per_symbol
- cooldown_sec, min_distance_between_entries_atr
- break_even_r=0.6, trail_start_r=1.0, trail_atr_mult
Emergency:
- emergency_sl_buffer_atr, flip flag mapping
AI keys (ai block):
- entry_shift_max_atr (default 0.20)
- event_conf_cap_high (default 72)
- pos_sl_tighten_max_atr (default 0.30)
end note

state "config.py" : ✅ Done
note right of config.py
- Config loader helpers (project-wide)
end note

state "config_resolver.py" : ✅ Done
note right of config_resolver.py
- resolve_effective_config(): merges defaults/overrides, validates structure
end note

state "config.backup.before_sens_tune.json" : ✅ Done
state "config.backup.before_st_change.json" : ✅ Done
note right of backups
- Historical snapshots; not runtime
end note

state "check_env.py" : ✅ Done
note right of check_env.py
- Environment sanity checks
end note

----------------------------------------
C) Strategy Engine (Core)
----------------------------------------

state "Strategy_Engine" : ✅ Done
Strategy_Engine --> AI_Layer_ModeD
Strategy_Engine --> Execution_Controller
Strategy_Engine --> Monitoring_UI

state "engine.py" : ✅ Done
note right of engine.py
Primary role:
- generate_signal_package(): produce baseline trade candidates + context
Main functions (expected/observed):
- MTF structure: HH/HL/LH/LL, BOS/CHoCH
- Proximity scoring with ATR
- Breakout/structure gating
- Entry/SL/TP/RR calculation (min_rr=1.5)
- SuperTrend alignment gate (frozen)
Indicators/tools used:
- ATR(14), ADX(14), Bollinger Bands(20,2), RSI(14), SuperTrend
- Market structure BOS/CHoCH, Proximity (ATR distance)
- (Optional context fields): session, event_risk, liquidity wall
Dependencies:
- config.json, config_resolver.py
- MetaTrader5 (data feed)
end note

state "engine.py.final_backup" : ✅ Done
state "engine_old1.py" : ✅ Done
state "engine_old2.py" : ✅ Done
note right of backups
- Historical engine versions; not runtime
end note

state "strategy_profiles.py" : 🟡 In Progress
note right of strategy_profiles.py
- Profile presets (sideway_scalp etc.) need alignment with latest gates/thresholds
end note

----------------------------------------
D) AI Layer (Integrated)
----------------------------------------

state "AI_Layer_ModeD" : 🟡 In Progress
AI_Layer_ModeD --> Execution_Controller
note right of AI_Layer_ModeD
Status reason:
- Code integrated and selftest passes
- Live impact not verified yet because positions=0 (no trades executed)
- Next: choose A/B/C test path to validate gating + SL manager on real positions
end note

state "ai_mentor.py" : ✅ Done
note right of ai_mentor.py
Version: v2.1.1
Functions:
- evaluate(package): new trade bounded adjust (Mode D)
  Output blocks: execution / analysis / mentor
- evaluate_position_sl(package): tighten-only SL suggestion (Mode C helper)
Constraints:
- Direction locked (cannot change BUY/SELL)
- No lot sizing change
- Entry shift <= 0.20*ATR
- SL ATR window 1.2–1.8
- Event cap high: 72
- Tighten-only bounded by 0.30*ATR per step
end note

state "ai_bridge_v1.py" : ⏳ Pending
note right of ai_bridge_v1.py
- Planned bridge for external LLM API (OpenAI) if needed
- Currently using local/mock AI logic to stabilize pipeline
end note

state "ai_signal_tester.py" : 🟡 In Progress
note right of ai_signal_tester.py
- Needs update to test Spec v2 packages and position tighten-only path
end note

state "test_ai_api.py" : ⏳ Pending
note right of test_ai_api.py
- For real API integration; not active in current Mode D local
end note

state "ai_confirmation_telegram.py" : 🟡 In Progress
note right of ai_confirmation_telegram.py
- Confirmation/notification flow via Telegram; may be consolidated with telegram_notifier.py
end note

----------------------------------------
E) Execution + Live Trading (Core)
----------------------------------------

state "Execution_Controller" : 🟡 In Progress
note right of Execution_Controller
Why not ✅:
- Executor loop runs, MT5 connects
- But currently positions=0 (no live trade executed yet)
- Need validate: trade gating path, then verify multi-order + SL manager in live
end note

state "mentor_executor.py" : 🟡 In Progress
note right of mentor_executor.py
Version: v2.6.1
Responsibilities:
- Load effective config
- Call engine.generate_signal_package()
- Frozen gates: BOS + SuperTrend OK
- Safety guards: spread, stops_level, freeze_level
- Multi-order: max_positions_per_symbol, min_distance_between_entries_atr, cooldown
- AI call:
  - new trade evaluate() and Python re-validate constraints
  - position tighten-only evaluate_position_sl()
- Execute market order via MT5
- Hybrid SL manager (Mode C):
  - BE@0.6R, Trail>=1R, Emergency SL on flip
  - Uses .state/positions.json (ticket -> initial_risk)
Current issue observed:
- Live: positions_get(symbol='GOLD') == 0, so SL manager not yet exercised
end note

state "trade_close_monitor.py" : 🟡 In Progress
note right of trade_close_monitor.py
- Needs validation with v2.6.1 state store and close events
end note

state "trade_logger.py" : ✅ Done
note right of trade_logger.py
- Logs trade attempts/outcomes
Dependency:
- mentor_executor.py
end note

state "mt5_tester.py" : ✅ Done
note right of mt5_tester.py
- Connectivity/testing tool for MT5
end note

----------------------------------------
F) News / Event / Context (Support Layer)
----------------------------------------

state "news_filter.py" : 🟡 In Progress
note right of news_filter.py
- Event risk classification (e.g., FOMC_HIGH)
- Should feed context.event_risk into engine package
end note

state "news_api_test.py" : ✅ Done
state "test_news.py" : ✅ Done
state "test_news_api.py" : ✅ Done
state "mock_news.json" : ✅ Done
note right of mock_news.json
- Mock data for development
end note

----------------------------------------
G) API Server / Dashboard (Monitoring UI)
----------------------------------------

state "Monitoring_UI" : 🟡 In Progress
Monitoring_UI --> Execution_Controller

state "api_server.py" : 🟡 In Progress
note right of api_server.py
- Serves signal preview / telemetry endpoints
- Must align payload fields with Spec v2 (execution/analysis/mentor)
end note

state "dashboard.py" : 🟡 In Progress
state "dashboard.html" : ✅ Done
state "templates/dashboard.html" : ✅ Done
state "dashboard_back_up.html" : ✅ Done
state "intelligent_dashboard.py" : 🟡 In Progress
state "intelligent_dashboard back up.py" : ✅ Done
note right of dashboard
- UI exists but needs wiring to newest states/logs
end note

----------------------------------------
H) Performance / Analytics / Risk (Future Completion)
----------------------------------------

state "Performance_Tracking" : 🟡 In Progress
Performance_Tracking --> Portfolio_Risk_Engine

state "performance_tracker.py" : 🟡 In Progress
note right of performance_tracker.py
- Rolling metrics, expectancy, drawdown (partial)
- Needs consistent ingestion from trade_logger + MT5 history
end note

state "Portfolio_Risk_Engine" : ⏳ Pending
note right of Portfolio_Risk_Engine
Planned:
- max daily DD guard
- exposure control, correlation risk
- volatility adjusted sizing (future; currently AI cannot change lot)
end note

----------------------------------------
I) Telegram / Notifications (Support)
----------------------------------------

state "telegram_notifier.py" : 🟡 In Progress
note right of telegram_notifier.py
- Used by executor for alerts
- Needs verify token/chat_id and that sends are not swallowed
end note

state "test_apis.py" : ✅ Done
note right of test_apis.py
- Integration smoke tests (various)
end note

================================================================================
III) TECHNICAL INDICATORS + TOOLS USED (ACTIVE VS PLANNED)
================================================================================

state "Indicators_Active" : ✅ Done
note right of Indicators_Active
Primary indicators (engine):
- ATR(14)
- ADX(14)
- Bollinger Bands(20,2)
- RSI(14)
- SuperTrend
Price-action / structure:
- HH/HL/LH/LL
- BOS / CHoCH
- Proximity scoring (ATR-distance)
Execution metrics:
- Spread (points), stops_level, freeze_level
Context (optional, if supplied):
- session (London/NY)
- event_risk (e.g., FOMC_HIGH)
- liquidity wall
- correlations (portfolio)
end note

state "Tools_Active" : ✅ Done
note right of Tools_Active
- MetaTrader5 Python package (execution + positions_get/order_send)
- Telegram notifier
- JSON config resolver
- Optional Flask API server/dashboard (in progress)
end note

================================================================================
IV) DEPENDENCY MAP (WHO MUST BE READY BEFORE WHO)
================================================================================

[*] --> config.json
config.json --> config_resolver.py
config_resolver.py --> engine.py
engine.py --> mentor_executor.py
ai_mentor.py --> mentor_executor.py
telegram_notifier.py --> mentor_executor.py
trade_logger.py --> mentor_executor.py
news_filter.py --> engine.py (optional context enrichment)
api_server.py --> mentor_executor.py / engine.py (preview endpoints)
dashboard.py --> api_server.py / logs
performance_tracker.py --> trade_logger.py + MT5 history

note left of mentor_executor.py
mentor_executor is the main orchestrator:
- If engine package lacks context.atr or flags, then:
  - SL manager may skip
  - Emergency flip path may never trigger
end note

================================================================================
V) CURRENT ISSUES + WHY (LIVE STATUS)
================================================================================

state "Live_Positions_GOLD" : 🔴 Issues
note right of Live_Positions_GOLD
Observed:
- positions_get(symbol='GOLD') == 0
Impact:
- BE/TRAIL/AI-tighten SL cannot be validated in live
Likely causes:
- AI/constraints gate rejects frequently (min_rr / SL ATR window)
- confidence_threshold=70 too strict for current market
- engine not generating eligible BOS+SuperTrend signals
Next action requires choosing A/B/C test path
end note

state "StateStore_v2_6_1" : 🟡 In Progress
note right of StateStore_v2_6_1
Expected file:
- .state/positions.json (created when a position exists)
Current:
- Not created yet because no open positions
end note

================================================================================
VI) WHAT WAS THE LAST TASK WE DID (MOST IMPORTANT)
================================================================================

state "Last_Task" : ✅ Done
note right of Last_Task
Last task completed:
- Integrated AI Spec v2 Mode D + Hybrid SL Manager Mode C into executor
- Delivered versions:
  - ai_mentor.py v2.1.1
  - mentor_executor.py v2.6.1
Validated:
- ai_mentor --selftest passes (new trade + position tighten-only)
- mentor_executor runs and connects MT5
Blocking:
- No live positions -> cannot validate SL manager actions in production
end note

================================================================================
VII) NEXT STEP OPTIONS (MUST CHOOSE ONE)
================================================================================

state "Next_Step_A_Manual_Trade_Test" : ⏳ Pending
note right of Next_Step_A_Manual_Trade_Test
Goal:
- Force open 1 manual position in GOLD
- Verify:
  - .state/positions.json created
  - BE/TRAIL works
  - AI tighten-only triggers (high-risk + in-profit)
end note

state "Next_Step_B_Tune_AI_Gates" : ⏳ Pending
note right of Next_Step_B_Tune_AI_Gates
Goal:
- Find exact reject reasons:
  - sl_atr_range_fail, rr_below_min, confidence below threshold
- Tune with rationale:
  - thresholds, event cap, SL ATR window, confidence gating
end note

state "Next_Step_C_Production_Debug_Gates" : ⏳ Pending
note right of Next_Step_C_Production_Debug_Gates
Goal:
- Add deep debug logs for every gate:
  - BOS/ST gate, spread, stop/freeze, constraint validation, AI outputs
- Produce “why not traded” trace per loop
end note

================================================================================
END OF STATE DIAGRAM (TEXTUAL)
================================================================================