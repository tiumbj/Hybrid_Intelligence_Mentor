State Diagram.txt (รายละเอียดแบบคำบรรยาย)
0) จุดเริ่มต้นของระบบ (Root Flow)

[*] --> "Environment Ready"

note right of "Environment Ready": Python 3.13 + pytest 9.0.2 + MetaTrader5 module, MT5 terminal เปิดและ login แล้ว

"Environment Ready" --> "Config Ready"

"Config Ready" --> "Engine Signal Package Ready"

"Engine Signal Package Ready" --> "AI Confirm API Ready"

"AI Confirm API Ready" --> "Validator v1.0 Enforced"

"Validator v1.0 Enforced" --> "Executor Loop Stable"

"Executor Loop Stable" --> "Trade Execution (MT5)"

"Trade Execution (MT5)" --> "Logging + Telegram + Performance"

1) โครงสร้างไฟล์ (Project Inventory) + หน้าที่หลัก + สถานะ

รูปแบบบรรทัด:
state "FILE" : STATUS
note right/left: version, ทำอะไร, config ที่เกี่ยว, ตัวเลขล่าสุด/เหตุผล

A) Core Configuration & Bootstrapping

state "config.json" : 🟡 In Progress

note right: config หลักของระบบ (symbol=GOLD, mode, lot, execution_safety, ai.*)

current number: ai.api_url ต้องชี้ http://127.0.0.1:5000/api/ai_confirm (ทำแล้วในแชทนี้)

why: ยังต้องจูนค่า threshold/filter ให้มี candidate เข้าจริงใน live (ดู DEBUG_SKIP ต่อ)

state "config.py" : 🟡 In Progress

note right: เก็บ key/const บางส่วน (เคยมี issue import NEWS_API_KEY vs NEWS_API_KEYS)

why: ต้องทำให้สอดคล้องกับ tests และ config_resolver; ลด hardcode key ในไฟล์

state "config_resolver.py" : 🟡 In Progress

note right: รวมค่า config ให้เป็น effective config (override/merge)

dependency: ใช้โดย mentor_executor.py, api_server.py, dashboard.py

why: ต้องตรวจว่าโครงสร้าง config ครอบคลุม ai/engine/execution_safety ตามที่ระบบใช้จริง

state "check_env.py" : ✅ Done (คาดว่าใช้งานได้)

note right: ตรวจ dependency / env readiness (MT5, libs)

why: ใช้เป็น preflight ก่อนรัน live

B) Engine (Market Structure + Indicators + Signal)

state "engine.py" : 🟡 In Progress

note right: หัวใจ rule-based engine สร้าง signal package (direction/entry/SL/TP/RR/context)

technical indicators/tools (จากบริบทโปรเจกต์):

ATR, ADX

Bollinger Bands

(บาง flow มี BOS/CHoCH/Structure/Proximity)

MT5 price feed

dependency: ถูกเรียกโดย mentor_executor.py

current issue: signature ของ generate_signal_package() ไม่รับ keyword symbol= → แก้ฝั่ง executor ด้วย signature-adapter แล้ว

why: ต้องทำให้ signature แน่นอน (แนะนำให้ finalize interface ใน engine เพื่อไม่ต้อง adapter)

state "engine_old1.py" : ⏳ Pending (Legacy/Archive)
state "engine_old2.py" : ⏳ Pending (Legacy/Archive)
state "engine.py.final_backup" : ⏳ Pending (Backup/Archive)

note right: ใช้อ้างอิงย้อนหลัง ไม่ควรอยู่ใน production path

state "strategy_profiles.py" : 🟡 In Progress

note right: โปรไฟล์ strategy/mode (เช่น sideway_scalp) + parameter presets

dependency: engine/config_resolver

why: ต้อง sync กับ config.json และ rules ของ validator

C) AI Layer (Confirm-only)

state "ai_mentor.py" : 🟡 In Progress

note right: AIMentor (mock/rule-based) ให้ผล execution + mentor + confidence

current behavior: ใช้โดย api_server.py:/api/ai_confirm และ executor (ถ้ายังไม่เรียก bridge)

why: ต้องทำให้ output schema สอดคล้องกับ validator (CONFIRM/REJECT, conf, entry/sl/tp)

state "ai_bridge_v1.py" : ✅ Done (Version 1.1.0)

note right: client ฝั่งเรียก AI confirm endpoint + parse response + validate v1.0 (fail-closed)

config: config.json -> ai.api_url, ai.timeout_sec, policy เช่น min_rr, entry_shift_max_atr

current number: sample test ได้ confidence=81% และ confirm ผ่าน (แสดงว่าปลายทางตอบถูก)

why Done: ผ่าน import + ใช้งานจริงกับ /api/ai_confirm

state "ai_confirmation_telegram.py" : 🟡 In Progress

note right: ส่งผล AI confirmation ไป Telegram (หรือรับคำสั่งยืนยัน)

dependency: telegram_notifier, config

why: ต้องยืนยัน role ว่า “AI confirm-only” ไม่ block ระบบ

state "ai_signal_tester.py" : 🟡 In Progress

note right: ทดสอบสัญญาณ/AI decision แบบ offline/preview

state "ai_bridge_v1.py --> validator_v1_0.py" : ✅ Done (Dependency Ready)

note right: boundary enforcement (สำคัญที่สุด)

D) Validator (Safety Gate)

state "validator_v1_0.py" : ✅ Done (Version 1.0.0)

note right: กติกา schema + confirm-only enforcement (fail-closed)

rules (ตัวอย่างที่ล็อกไว้):

decision must be CONFIRM/REJECT

confidence 0..1

direction lock

lot lock

entry shift bounded by ATR

SL tighten-only

RR floor (>= 1.5)

current number: unit tests 11 cases ผ่านหมด

why Done: pytest 11 passed

state "tests/test_validator_v1_0.py" : ✅ Done (Version 1.0.0)

note right: deterministic unit tests สำหรับ validator

state "tests/conftest.py" : ✅ Done (Version 1.0.0)

note right: เพิ่ม project root เข้า sys.path เพื่อให้ pytest import ได้แน่นอน

why Done: pytest (ไม่ใช่ python -m pytest) ก็ผ่านแล้ว

state "pytest.ini" : ✅ Done

note right: testpaths=tests (เคยเจอ error เพราะไฟล์เริ่มด้วย """ แล้วแก้แล้ว)

E) API Server + Dashboard + Monitoring

state "api_server.py" : ✅ Done (Version 3.1.0)

note right: Flask API local

endpoints:

GET/POST /api/config

POST /api/mode/<mode_id>

GET /api/status

GET /api/signal_preview

GET /api/performance

POST /api/ai_confirm (เพิ่มล่าสุด)

why Done: ทำให้ ai_bridge_v1.py --sample confirm ได้จริง

state "dashboard.py" : 🟡 In Progress
state "dashboard.html" : 🟡 In Progress
state "templates/dashboard.html" : 🟡 In Progress
state "intelligent_dashboard.py" : 🟡 In Progress

note right: UI แสดงสถานะ/สัญญาณ/ผลการเทรด

dependency: api_server endpoints + performance_tracker

state "performance_tracker.py" : 🟡 In Progress

note right: สรุป performance, winrate, RR realized, drawdown

dependency: trade_logger + trade_close_monitor

F) Execution (MT5 Trading)

state "mentor_executor.py" : 🟡 In Progress (Version 2.7.2)

note right: loop หลักสำหรับ live

ล่าสุดทำ:

MT5 init success

แก้ call signature engine ด้วย adapter (ไม่ใช้ keyword symbol ถ้า engine ไม่รองรับ)

บังคับ validator ก่อน execute (fail-closed)

เพิ่ม DEBUG_SKIP logging (เพื่อรู้ว่าติด filter ตรงไหน)

current issue: ตอนนี้รันแล้วนิ่ง (คาดว่า engine ไม่ส่ง candidate หรือถูก filter) → งานถัดไปคือดู DEBUG_SKIP แล้วจูน config/engine

state "mt5_tester.py" : 🟡 In Progress

note right: ทดสอบการเชื่อม MT5 / order send / symbol info

state "trade_logger.py" : 🟡 In Progress

note right: เก็บ log trade + snapshot สำหรับ validation/performance

state "trade_close_monitor.py" : 🟡 In Progress

note right: ติดตามปิดออเดอร์, อัปเดต realized PnL, feed ให้ performance

G) News Filter (optional / auxiliary)

state "news_filter.py" : 🟡 In Progress
state "news_api_test.py" : 🟡 In Progress
state "test_news.py" : 🟡 In Progress
state "test_news_api.py" : 🟡 In Progress
state "test_news_api.py / mock_news.json" : 🟡 In Progress

note right: ใช้กรองความเสี่ยงข่าว (event risk)

เคยเจอ issue import: NEWS_API_KEY vs NEWS_API_KEYS → ต้อง normalize ให้ตรงทุกที่

H) Telegram / Notification

state "telegram_notifier.py" : 🟡 In Progress

note right: ส่งข้อความสถานะ/ดีล/เหตุผล block ไป Telegram

dependency: config.json (token/chat_id)

I) Tests & Tools / Utilities

state "test_ai_api.py" : 🟡 In Progress
state "test_apis.py" : 🔴 Issues

note right: เคย fail ตอน collection เพราะ import NEWS_API_KEY ไม่ตรงชื่อ

action: align config.py + tests ให้ตรง (rename หรือ provide compatibility export)

state "test_apis.py (analysis_out/…)" : 🔴 Issues

note right: มีผลซ้ำใน path อื่นตอนรัน pytest ครั้งแรก (เคลียร์ด้วย testpaths แล้ว แต่ไฟล์นี้ยังควรแก้/ลบ/ย้าย)

state "remove_emoji.py" : ⏳ Pending
state "bu.txt" : ⏳ Pending
state "Code Run Test.txt" : 🟡 In Progress
state "README_TH.md" : 🟡 In Progress
state "instructions.txt / instructions.html / Run procedure.html" : 🟡 In Progress

note right: เอกสาร runbook + how-to สำหรับ dev ใหม่

2) Dependency Map (ไฟล์ไหนต้องเสร็จก่อน)
Core dependencies (ต้องเสร็จก่อนถึงจะ live ได้จริง)

[*] --> config.json

config.json --> engine.py

engine.py --> mentor_executor.py

validator_v1_0.py + tests/ --> ai_bridge_v1.py*

ai_mentor.py --> api_server.py (/api/ai_confirm)

api_server.py --> ai_bridge_v1.py (ai.api_url)

ai_bridge_v1.py + validator --> mentor_executor.py (confirm gate)

mentor_executor.py --> trade_logger.py / telegram_notifier.py / performance_tracker.py

Transitions (การเปลี่ยนสถานะหลัก)

"engine.py" (🟡) --> ✅ เมื่อ:

interface generate_signal_package() finalize (รับ symbol/mode แบบแน่นอน)

signal package schema stable (direction, entry_candidate, stop_candidate, tp_candidate, rr, context.atr)

"mentor_executor.py" (🟡) --> ✅ เมื่อ:

DEBUG_SKIP บอกเหตุผลแล้วจูนจนมี trade candidate (ในสภาพ live)

มีอย่างน้อย 1 trade ผ่าน validator + ส่ง MT5 order สำเร็จ

"test_apis.py" (🔴) --> ✅ เมื่อ:

แก้ export/import key ให้ตรงกัน (NEWS_API_KEY vs NEWS_API_KEYS)

3) Technical Indicators / Tools ที่ใช้ (ตามระบบจริง)
Indicators (แกนตัดสินใจ)

ATR: ใช้คำนวณ volatility, proximity, entry shift bound, SL distance bounds

ADX: กรอง trend strength

Bollinger Bands: กรองสภาวะ overextension / squeeze / breakout

Structure (BOS/CHoCH): ยืนยันการกลับตัว/เบรกโครงสร้าง

(Optional) SuperTrend: บาง flow เคยระบุเป็นตัวช่วย

Tools / Infra

MetaTrader5 Python API: ราคา, symbol info, order_send, positions

Flask API Server: /api/ai_confirm และ endpoints อื่น

pytest: unit tests validator + regression

Telegram: แจ้งเตือน

Logging/Performance: trade_logger, performance_tracker

4) Current Numbers (สิ่งที่ “ตัวเลขล่าสุด” และเหตุผล)

Validator tests: 11 passed → ยืนยัน rule gate ใช้งานได้จริง

AI confirm sample: confidence ~81% (จาก AIMentor) → schema v1.0 ถูกต้อง

MT5 initialize: True / Success → execution environment พร้อม

Executor loop: ไม่ error แล้ว แต่ “ยังไม่เห็น trade” → ต้องอ่าน DEBUG_SKIP เพื่อรู้ filter ที่บล็อกอยู่ (งานถัดไป)

5) สถานะรวมเป็นภาพใหญ่ (Nested States)
state "Phase 1: Safety Foundation" : ✅ Done

validator_v1_0.py, tests/, pytest.ini, ai_bridge_v1.py, api_server.py:/api/ai_confirm

state "Phase 2: Live Loop Stabilization" : 🟡 In Progress

mentor_executor.py (v2.7.2), engine.py interface stabilization, config tuning, DEBUG_SKIP analysis

state "Phase 3: Monitoring & Ops" : 🟡 In Progress

trade_logger.py, performance_tracker.py, trade_close_monitor.py, dashboard.py, telegram_notifier.py

state "Phase 4: News Risk Layer" : 🟡 In Progress / 🔴 Issues

news_filter.py + tests (แก้ key import)

6) Command Order (ใช้งานจริง) สำหรับ dev คนถัดไป
Run tests
cd C:\Hybrid_Intelligence_Mentor
pytest
Run API server
python api_server.py
Run AI bridge sample
python ai_bridge_v1.py --config config.json --sample
Run executor
python mentor_executor.py
Git workflow
git status
git add .
git commit -m "message"
git push origin main

Repo: https://github.com/tiumbj/Hybrid_Intelligence_Mentor

7) งานถัดไป (Next Developer Must Do)

เปิด mentor_executor.py (v2.7.2) แล้วรันดู DEBUG_SKIP | ...

ระบุว่า block เพราะ:

no_candidate_direction_NONE (engine ไม่ปล่อยสัญญาณ)

spread_block / confidence_block / stop_level_block

ai_block validator_reject:...

จูน config.json และ/หรือ engine.py ให้ได้ candidate จริงใน live

ทำ 1 trade ผ่าน gate เพื่อยืนยัน end-to-end (Engine→AI→Validator→MT5→Log/Telegram)