State Diagram.txt — Hybrid Intelligence Mentor (HIM)
0) Project Overview State

Project: Hybrid_Intelligence_Mentor
Goal State: Candidate Export > 0 (Replay) → Validate Quality → (Optional) AI Confirm → Live Execution MT5

Current Global Status (ล่าสุด):

✅ Replay Infrastructure: เสถียรแล้ว (แก้ MT5Shim semantics pos=0 → tail(count) สำเร็จ)

🔴 Candidate Export: ยัง = 0 (Engine ยังไม่ให้ BUY/SELL ใน replay)

ขั้นถัดไปที่ต้องทำ: เปิด debug-first เพื่ออ่าน blocked_by ใหม่ แล้วปรับ logic gate ตามสาเหตุ

Key Versions/Numbers (ล่าสุดที่ยืนยันแล้ว):

candidate_replay_runner_v1_1_0.py → Runner v1.1.1 (shim semantics fix)

engine.py → Engine v2.10.5

Replay run ล่าสุด:

Range: 2025-01-01 → 2025-01-10

LTF: M5 bars ≈ 1801

Warmup: 800

Result: Exported=0, Eval errors=0

1) Standard Project Structure (Files Inventory)
Root (Core / Execution)

engine.py

mentor_executor.py

ai_mentor.py

ai_bridge_v1.py

api_server.py

dashboard.py

intelligent_dashboard.py

trade_logger.py

trade_close_monitor.py

telegram_notifier.py

ai_confirmation_telegram.py

performance_tracker.py

strategy_profiles.py

news_filter.py

config.py

config_resolver.py

check_env.py

Replay / Testing / Validation

candidate_replay_runner_v1_1_0.py (ปัจจุบัน v1.1.1 ในไฟล์เดียวกัน)

ai_signal_tester.py

mt5_tester.py

news_api_test.py

test_ai_api.py

test_apis.py

test_news.py

test_news_api.py

Config / Docs / UX

config.json

config.backup.before_sens_tune.json

config.backup.before_st_change.json

README_TH.md

instructions.txt

instructions.html

Run procedure.html

Code Run Test.txt

dashboard.html

dashboard_back_up.html

templates/dashboard.html

stateDiagram-v2.txt

stateDiagram-v3.txt

Legacy / Backup

engine_old1.py

engine_old2.py

engine.py.final_backup

mentor_executor.py.backup2

engine.py.final_backup

bu.txt

remove_emoji.py

.gitignore

2) Technical Indicators & Tools (System-Wide)
Indicators (ใช้ใน Engine / Filters)

ATR (risk sizing, SL/TP distance, proximity normalization)

ADX (regime gate: sideway vs trending)

Bollinger Bands (touch/near trigger, width normalized by ATR)

RSI (overbought/oversold + soft band)

SuperTrend (trend conflict filter / direction confirmation)

Market Structure (BOS/CHoCH/HH-HL/LH-LL) (bias + trigger)

(Optional) OB/FVG (helper zones) — แล้วแต่ implementation ใน engine/strategy_profiles

Tools / Libraries / Integration

MetaTrader5 Python API

MT5Shim (ภายใน replay runner) สำหรับจำลอง copy_rates_from_pos

Flask API (api_server.py)

Telegram notifier/confirm modules

JSON config resolver + profile switching

3) Per-File State Nodes (Purpose + Config + Status + Dependencies)

Format ต่อไฟล์:
State "filename" : STATUS

Main Function

Config (key fields / numbers / why)

Current Findings (ล่าสุด/ตัวเลข)

Dependencies (ต้องพึ่งไฟล์ไหนก่อน)

Notes (สิ่งที่ต้องทำต่อ)

3.1 Core Engine Layer

State "engine.py" : 🟡 In Progress

Main Function: Core trading logic: MTF structure + indicators + regime gate + signal package output (direction, blocked_by, etc.)

Config / Numbers ที่สำคัญ:

mode / profiles.<mode> / timeframes (HTF/MTF/LTF)

sideway_scalp.adx_max (เดิม 22 ปรับเป็น 30 เพื่อผ่าน regime gate)

sideway_scalp.bb_width_atr_max (6.0; อาจยังต้องผ่อน)

profiles.sideway_scalp.min_rr (1.5; อาจยังเข้ม)

atr_sl_mult (1.6)

require_confirmation (ถูกปิดแล้วเพื่อเพิ่ม candidate)

proximity_score_min (0.4)

Current Findings: replay ล่าสุดไม่มี error แต่ยังไม่ส่ง BUY/SELL → direction=NONE ตลอด (ต้องดู blocked_by ใหม่หลัง shim fix)

Dependencies: config.json, strategy_profiles.py, MT5 API (live) หรือ MT5Shim (replay)

Notes: ขั้นถัดไปคืออ่าน blocked_by ที่แท้จริงจาก replay debug-first แล้วปรับ gate ที่ปิดอยู่

State "strategy_profiles.py" : 🟡 In Progress

Main Function: รวม profile logic ต่อ mode (sideway_scalp/swing/ฯลฯ), parameter sets, gating rules

Config / Numbers: ผูกกับ profiles.* ใน config.json

Current Findings: ต้องตรวจว่า profile sideway_scalp ใช้ค่า top-level หรือ profile-level และลำดับ override ถูกต้อง

Dependencies: config.json, engine.py

Notes: ถ้า engine ยังอ่าน param จาก top-level ให้ทำให้ชัด (single source of truth)

3.2 Replay / Dataset Layer (Critical Path)

State "candidate_replay_runner_v1_1_0.py (v1.1.1)" : 🟡 In Progress (Infrastructure Done, Logic Pending)

Main Function: Replay engine แบบ bar-by-bar, export candidate events เป็น .jsonl เมื่อ direction เป็น BUY/SELL

Config / Numbers:

Run ล่าสุด: --start 2025-01-01 --end 2025-01-10 --warmup 800 --max-events 200

Output: backtests/candidates_GOLD_M5_...jsonl

v1.1.1: แก้ MT5Shim copy_rates_from_pos(pos=0) ให้คืน tail(count) (ตรงกับ MT5 semantics)

Current Findings:

ก่อนแก้: blocked_by=no_data ตลอด

หลังแก้: no_data หาย, Eval errors=0 แต่ Exported=0

Dependencies: engine.py, config.json, MT5 หรือ rate source + shim

Notes (Next): ต้องรันด้วย --debug-first 30 เพื่อดู blocked_by ใหม่ → แล้วแก้ engine gate

State "mt5_tester.py" : ✅ Done

Main Function: ทดสอบเชื่อม MT5, ดึงข้อมูล, sanity check symbol/timeframes

Dependencies: MetaTrader5, config

Notes: ใช้ยืนยันว่า live MT5 OK

3.3 Execution Orchestrator

State "mentor_executor.py" : 🟡 In Progress

Main Function: ตัวรันหลักประสาน Engine → (AI confirm optional) → execute order → notify

Config / Numbers: ใช้ config.json เป็นหลัก, โหมด mock/live

Dependencies: engine.py, ai_mentor.py, telegram_notifier.py, trade_logger.py, MT5

Notes: ถือว่าพร้อมระดับหนึ่ง แต่ขึ้นกับ engine signal reliability

3.4 AI Layer (Confirm-only)

State "ai_mentor.py" : 🟡 In Progress

Main Function: สร้างคำอธิบาย/คอนเฟิร์มสัญญาณ (ตามระบบ confirm-only)

Dependencies: ai_bridge_v1.py, api_server.py (ถ้าเรียกผ่าน API)

Notes: ไม่ใช่คอขวดตอนนี้

State "ai_bridge_v1.py" : 🟡 In Progress

Main Function: ส่ง/รับ payload กับ AI endpoint, ลด token cost, normalize response

Dependencies: network/API keys

Notes: ไม่ใช่คอขวดตอนนี้

State "ai_confirmation_telegram.py" : 🟡 In Progress

Main Function: AI confirmation ผ่าน Telegram UI flow

Dependencies: Telegram bot, notifier

Notes: optional ตามโหมด

3.5 API / Dashboard

State "api_server.py" : 🟡 In Progress

Main Function: REST API ให้เรียก signal preview / dry_run_order / status

Dependencies: engine.py, config

Notes: ดีสำหรับ debug live

State "dashboard.py" / "intelligent_dashboard.py" : 🟡 In Progress

Main Function: UI แสดงสถานะ signal / positions / logs

Dependencies: templates, api

Notes: ไม่ใช่คอขวดตอนนี้

3.6 Risk / Logging / Ops

State "trade_logger.py" : ✅ Done

Main Function: log trades + debug context

Dependencies: engine/MT5 execution

State "trade_close_monitor.py" : 🟡 In Progress

Main Function: monitor ปิดออเดอร์, trailing/exit rules

Dependencies: MT5, trade_logger

State "performance_tracker.py" : 🟡 In Progress

Main Function: เก็บสถิติ winrate, expectancy, drawdown

Dependencies: trade logs / dataset

3.7 News Filter (Optional)

State "news_filter.py" : ⏳ Pending

Main Function: กรองข่าวแรง/ช่วงเสี่ยง

Dependencies: news api / mock_news.json

Notes: ไม่ใช่คอขวดตอนนี้

3.8 Config / Resolver

State "config.json" : 🟡 In Progress (Active Tuning)

Main Function: single source config สำหรับ mode/profiles/timeframes/thresholds

Current Key Values (ที่มีผลต่อ candidate ตอนนี้):

mode: sideway_scalp

timeframes: H1/M15/M5

sideway_scalp.adx_max: แก้ให้ top-level = 30 (เดิมซ้ำ 22/30)

require_confirmation: false

proximity_score_min: 0.4

touch_buffer_atr: 0.2

near_trigger_atr: 0.9 (ยังอาจเพิ่มเป็น 1.2)

min_rr: 1.5 (อาจลดเป็น 1.1 เพื่อเพิ่ม candidate)

Dependencies: engine.py, strategy_profiles.py

Notes: ต้องทำให้การ override ชัดเจน (เลิกซ้ำค่า 2 จุด)

State "config_resolver.py" / "config.py" : ✅ Done

Main Function: โหลด config + merge/validate defaults

Notes: ใช้ยืนยันว่า engine อ่านค่าที่แก้จริง

3.9 Documentation / State Tracking

State "stateDiagram-v2.txt" : ✅ Done
State "stateDiagram-v3.txt" : 🟡 In Progress

Main Function: ติดตาม progression ของระบบ

Dependencies: none

Notes: version ล่าสุดต้องสะท้อน “Shim fixed แต่ Candidate ยัง 0”

State "README_TH.md" : 🟡 In Progress

Main Function: คู่มือไทย

Notes: ควรเพิ่ม “Replay debug flow” + “blocked_by table”

4) Dependencies (File-to-File Graph in Words)
Core dependency chain (Critical Path)

config.json → 2. config_resolver.py/config.py → 3. engine.py → 4. candidate_replay_runner_v1_1_0.py → 5. backtests/*.jsonl

Execution chain (Live)

config.json → engine.py → mentor_executor.py → MT5 orders
↳ trade_logger.py, trade_close_monitor.py
↳ optional ai_mentor.py/ai_bridge_v1.py
↳ optional telegram_notifier.py

UI/API chain

api_server.py → engine.py → dashboard.py / templates/dashboard.html

5) State Transitions (ข้อความแบบ State Diagram)
Global state machine

[*] → Setup

Setup: ตรวจ MT5 / config / environment

Setup → ReplayReady

เมื่อ replay runner + shim + preload ข้อมูลได้

ReplayReady → ShimFixed ✅

เมื่อแก้ copy_rates_from_pos semantics (pos=0 → tail) สำเร็จ

ShimFixed → CandidateGateDebug 🟡 (ปัจจุบัน)

เหตุการณ์: Exported=0 แต่ Eval errors=0

CandidateGateDebug → CandidateExported ✅

เงื่อนไข: Exported > 0

CandidateExported → DatasetValidation

validate distribution, blocked_by ratios, quality

DatasetValidation → StrategyTune

tune thresholds (ADX/BBWidth/Proximity/min_rr)

StrategyTune → LiveShadow

dry-run/preview

LiveShadow → LiveExecution

execute orders

LiveExecution → PerformanceTracking

stats loop

Nested: CandidateGateDebug (Current)

State: “blocked_by unknown (new)”

Transition:

Run --debug-first 30 → อ่าน blocked_by:

ถ้า not_sideway_adx → เพิ่ม adx_max

ถ้า not_sideway_bbwidth → เพิ่ม bb_width_atr_max

ถ้า low_proximity_score → ลด proximity_score_min / เพิ่ม near_trigger_atr

ถ้า rr_too_low → ลด min_rr หรือเพิ่ม RR model

ถ้า no_confirmation → (ตอนนี้ปิดแล้ว) ผ่าน

6) Notes (สำคัญสำหรับ dev คนถัดไป)
Note Right of “candidate_replay_runner”

Runner v1.1.1 แก้ shim semantics แล้ว

ตอนนี้ “ไม่ใช่ no_data” อีกต่อไป

แต่ยังไม่รู้ “blocked_by ใหม่” เพราะยังไม่ได้รัน --debug-first

Note Left of “engine.py”

Engine v2.10.5 มี regime gate สำหรับ sideway_scalp: ADX + BBWidth/ATR

มี quality gate: proximity score, confirmation, RR

ต้องใช้ evidence จาก blocked_by เท่านั้น ไม่เดา

7) Last Task ที่ทำล่าสุด (ต้องจำให้ AI ตัวถัดไปรู้)

แก้ candidate_replay_runner_v1_1_0.py เป็น v1.1.1

Fix MT5Shim.copy_rates_from_pos(pos=0) ให้คืน tail(count) (ตรง MT5)

Replay run หลังแก้: Exported=0, Eval errors=0 → Infrastructure ผ่านแล้ว

งานถัดไป (ยังไม่ทำ): รัน --debug-first 30 เพื่อหา blocked_by ใหม่ แล้วจูน engine/config

8) Command Order ที่ใช้จริง (Git + Run)
Run replay
python .\candidate_replay_runner_v1_1_0.py --config .\config.json --start 2025-01-01 --end 2025-01-10 --max-events 200 --warmup 800 --debug-first 30
Git workflow (repo: https://github.com/tiumbj/Hybrid_Intelligence_Mentor
)
git status
git add .
git commit -m "Fix MT5Shim copy_rates_from_pos semantics (pos=0 -> tail(count))"
git push origin main
9) Status Summary Table (ย่อ)

✅ Done: mt5_tester.py, trade_logger.py, config_resolver.py/config.py (พื้นฐาน)

🟡 In Progress: engine.py, strategy_profiles.py, mentor_executor.py, api_server.py, dashboard.py, candidate_replay_runner_v1_1_0.py (logic gate)

🔴 Issues: Candidate Export = 0 (ต้องอ่าน blocked_by ใหม่)

⏳ Pending: news_filter.py (optional), advanced validation, performance tracking full loop

ถ้าต้องการ ผมสามารถปรับ “State Diagram.txt” ให้ตรงกับ stateDiagram-v2/v3 ที่คุณมีใน repo (โดยให้คุณ paste เนื้อหาไฟล์ v3 มา 1 ครั้ง) แล้วผมจะ sync version/progression ให้ตรงกันทันที โดยไม่เดา.